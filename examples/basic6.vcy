int f(int x) {
    int y = x;
    if (x < 2) {
        return 1;
    }
    y = y * f(x - 1);
    return y;
}

int g(int k) {
    if (k < 2) {
        return 1;
    } else {
        return k * g(k - 1);
    }
}

int main(int argc, string[] argv) {
    int x = 0;
    x = x + f(6);
    return x;
}