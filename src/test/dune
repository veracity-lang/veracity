(include_subdirs no)

(test
    (name test)
    (libraries ounit2 vcy)
    (modules test)
    (action (setenv LD_LIBRARY_PATH .. (run %{test})))
)

(test
    (name api_test)
    (libraries ounit2 vcy)
    (modules api_test)
    (action (setenv LD_LIBRARY_PATH .. (run %{test})))
)
